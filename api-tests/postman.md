# API Testing — TaskFlow

## Endpoints tested
- POST /login
- GET /tasks
- POST /tasks
- PUT /tasks/{id}

## Observations
- Status codes validated (200, 400, 401)
- Response structure checked
- Authorization tested via token
